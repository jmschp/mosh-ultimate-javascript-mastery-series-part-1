// 11- Exercise 2- Landscape or Portrait

// Create a function that returns true if a images is in landscape format and false if is in portrait format.


function isLandscape(width, height) {
    return width > height // ? true : false; we don't need this last parte because the evaluation of the condition is already returning true or false.
}

console.log(isLandscape(800, 600))